parameters:
    church_user.verify_email.class: Church\UserBundle\Service\VerifyEmail

services:
    church_user.verify_email:
        class: %church_user.verify_email.class%
        arguments: [@hip_mandrill.dispatcher, @doctrine.orm.entity_manager, @router]
